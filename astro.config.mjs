import { defineConfig } from 'astro/config';

export default defineConfig({
  // Cuando la web tenga dominio propio, definir `site` aquí
  // (necesario para sitemap y URLs canónicas):
  // site: 'https://tudominio.cat',
});
